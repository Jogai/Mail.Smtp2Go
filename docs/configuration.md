# Configuration

Covers `Smtp2GoClientOptions` (base URL, region, authentication scheme, timeouts, client-side validation) and the `AddSmtp2Go` registration from `Scott.Mail.Smtp2Go.DependencyInjection`. Written in plans 02 and 05.
